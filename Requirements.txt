fastapi
uvicorn
pandas
numpy
yfinance
pydantic
import os
import uvicorn
from fastapi import FastAPI

app = FastAPI()

if __name__ == "__main__":
    # Cloud Run MUST have this line to work
    port = int(os.environ.get("PORT", 8080))
    uvicorn.run(app, host="0.0.0.0", port=port)
